# Asset this pipeline produces
asset_key: ["lakehouse", "test", "test_trino_batching_trino"]

# Dependencies (asset keys this depends on)
# depends_on: []

# Job-level concurrency (max concurrent ops in this job)
job_concurrency: 1

# Job retry configuration
job_retry:
  max_attempts: 3          # Maximum retry attempts
  max_delay: 1h            # Maximum delay (1 hour)

# Pipeline steps - three-step ETL with dependencies
steps:
  # Step 1: Extract from Trino in batches
  - name: extract
    executor: trino_extract
    outputs: ["df"]
    concurrency_key: "trino_extract"  # Limit concurrent Trino extract operations
    config:
      host: {{ trino.host }}
      port: {{ trino.port }}
      user: {{ trino.user }}
      select_query: SELECT * FROM {{ trino.catalog }}.{{ trino.schema }}.test_source
      batch_size: 50
      pk: id
      retry:  # Custom retry configuration
        max_attempts: 3  # Try up to 5 times instead of default 3
        base_delay: 1s    # Wait 1s, 2s, 4s, 8s, 16s
        max_delay: 1m     # Cap delay at 1 minute

  # Step 3: Load transformed data into {{ trino.schema }}_d
  - name: load
    executor: trino_load
    inputs: ["df"]
    depends_on: ["extract"]  # Depends on transform step
    concurrency_key: "trino_load"  # Limit concurrent Trino load operations
    config:
      host: {{ trino.host }}
      port: {{ trino.port }}
      user: {{ trino.user }}
      catalog: {{ trino.catalog }}
      schema: {{ trino.schema }}
      table: test_trino_batching_trino
      retry:  # Custom retry configuration
        max_attempts: 3  # Try up to 5 times instead of default 3
        base_delay: 1s    # Wait 1s, 2s, 4s, 8s, 16s
        max_delay: 1m     # Cap delay at 1 minute
